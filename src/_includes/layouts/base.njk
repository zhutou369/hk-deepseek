<!DOCTYPE html>
<html lang="zh-HK">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="baidu-site-verification" content="codeva-1zDnTk8YZ4">
  {% set docTitle = title or site.title %}
  {% set metaDescription = description or site.description %}
  {% set canonicalPath = canonicalUrl or page.url or "/" %}
  {% set canonical = site.url + canonicalPath %}
  {% set shareImage = (ogImage or coverImage or site.ogImage or "/static/og-default.png") | assetUrl %}
  {% set shareImageAbs = site.url + shareImage %}
  <title>{% if page.url != "/" %}{{ docTitle }} | {{ site.brand }}{% else %}{{ site.title }}{% endif %}</title>
  <meta name="description" content="{{ metaDescription }}">
  <meta name="keywords" content="{{ pageKeywords or site.keywords }}">
  {% if noindex %}
  <meta name="robots" content="noindex,follow">
  {% else %}
  <meta name="robots" content="index,follow,max-image-preview:large">
  {% endif %}
  <link rel="canonical" href="{{ canonical }}">
  <meta property="og:type" content="{% if layout == 'layouts/post.njk' %}article{% else %}website{% endif %}">
  <meta property="og:title" content="{{ docTitle }}">
  <meta property="og:description" content="{{ metaDescription }}">
  <meta property="og:url" content="{{ canonical }}">
  <meta property="og:site_name" content="{{ site.brand }}">
  <meta property="og:locale" content="{{ site.locale }}">
  <meta property="og:image" content="{{ shareImageAbs }}">
  <meta property="og:image:alt" content="{{ docTitle }}">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:image" content="{{ shareImageAbs }}">
  <script type="application/ld+json">
{{ {
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": site.brand,
  "url": site.url,
  "description": site.description
} | dump | safe }}
  </script>
  <link rel="stylesheet" href="/css/style.css">
  <script charset="UTF-8" id="LA_COLLECT" src="//sdk.51.la/js-sdk-pro.min.js" defer></script>
  <script>window.addEventListener("load",function(){if(window.LA){LA.init({id:"{{ site.laId }}",ck:"{{ site.laId }}"})}})</script>
</head>
<body>
  <header class="site-header">
    <div class="container header-nav">
      {% if page.url == "/" %}
        <div class="logo" style="margin:0;font-size:inherit;font-weight:inherit;"><a href="/">{{ site.brand }}</a></div>
      {% else %}
        <div class="logo"><a href="/">{{ site.brand }}</a></div>
      {% endif %}
      <nav>
        <ul class="nav-links">
          <li><a href="/" {% if page.url == "/" %}class="active"{% endif %}>首頁</a></li>
          <li><a href="/posts/" {% if page.url.startsWith('/posts/') %}class="active"{% endif %}>技術文章</a></li>
          <li><a href="/about/" {% if page.url.startsWith('/about/') %}class="active"{% endif %}>關於</a></li>
        </ul>
      </nav>
    </div>
  </header>
  <main>{{ content | safe }}</main>
  <footer class="site-footer">
    <div class="container">
      <div class="copy">
        <p>&copy; 2026 {{ site.brand }} · 本站為第三方使用說明與技術整理，非 DeepSeek 官方網站。<a href="/about/" style="color:#93c5fd;margin-left:8px;">關於本站</a></p>
      </div>
    </div>
  </footer>
</body>
</html>
